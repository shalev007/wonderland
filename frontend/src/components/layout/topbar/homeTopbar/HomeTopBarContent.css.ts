import { style } from '@vanilla-extract/css';

export const centerGroup = style({
  display: 'flex',
  alignItems: 'center',
});

export const leftGroup = style({
  display: 'flex',
  alignItems: 'center',
  gap: '12px',
});
